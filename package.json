{
  "name": "shortcuts-reference",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --enable-logging",
    "package": "electron-packager release-builds/ ALTA --platform=darwin --arch=all --out=release-builds/mac  --icon=src/icons/package.icns --overwrite"
  },
  "author": "Hanne Maes",
  "license": "ISC",
  "devDependencies": {
    "electron": "^7.1.1",
    "electron-packager": "^14.1.0"
  }
}
